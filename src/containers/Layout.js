/** @format */

import React from 'react';
import SideDrawer from '../components/SideDrawer/SideDrawer';

const Layout = () => {
  return (
    <div>
      <SideDrawer />
    </div>
  );
};

export default Layout;
